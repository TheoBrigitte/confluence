version: 2
jobs:
  server:
    docker:
      - image: circleci/buildpack-deps
    steps:
      - checkout
      - setup_remote_docker
      - run: make package-server
workflows:
  version: 2
  build:
    jobs:
      - server
